/** @type {import('next').NextConfig} */
const nextConfig = {
  // 1. 아래 줄이 있다면 반드시 삭제하거나 앞에 // 를 붙여서 주석 처리하세요!
  // output: 'export', 

  reactStrictMode: true,
  // 나머지 설정들...
}

module.exports = nextConfig